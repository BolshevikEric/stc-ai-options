## 第一步：先交付预览页（你确认效果后才动插件代码）

新建 `F:\stc-ai-options\preview.html`（自包含单文件，双击即可在浏览器打开，不影响插件本身）：

- **完整模拟插件选项框**：模拟聊天背景 + 几条对话气泡 + 底部选项框，样式与 style.css 现状一致（含深/浅主题变量）
- **演示目标交互**：
  - 「生成选项」主按钮占 88% 宽，右侧同行 12% 宽「生成方向」按钮（🧭 罗盘图标 + 文字）
  - 点击「生成方向」→ 选项框内展开输入行（预填当前方向 + ✓ 确认 + ✕ 取消，回车确认 / Esc 取消）
  - 确认后立即"生成"（模拟 1 秒延迟），生成结果按方向变化（如输入"悬疑"出悬疑向选项）
  - 「换一批」沿用当前方向；已设方向时按钮高亮
  - 留空确认 = 清除方向
- **三种状态可看**：未生成入口态 / 生成中 / 已生成（选项卡片+换一批）/ 失败重试态
- **自适应演示**：顶部工具栏可切换「PC 宽度 / 手机宽度 / 跟随窗口」+「深色 / 浅色主题」；手机宽度下「生成方向」只显示图标、触控目标 40px
- 侧边实时显示当前状态与"注入到提示词的内容"，方便理解行为

预览页确认效果后，再按已定方案实施：index.js（makeDirButton / buildDirInputRow / confirmDirection 三个函数 + renderBar 三分支包 `.stc-co-pair` 容器 + generateOptions 注入 `{{direction}}` 或末尾追加方向句）、style.css（pair/dir/dir-row 样式 + 768px 媒体查询）、README.md 补文档；最后 `node --check index.js` 验证。